name: Refresh New Recruit
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Refresh New Recruit
        run: curl --request GET "https://www.newrecruit.eu/api/check-for-update/BSData/horus-heresy-3rd-edition"
